#----------------------对称加密----------------------


#encrypt:
#  key: didispace #对称加密key



#----------------------非对称加密 RSA密钥对----------------------
encrypt:
  key-store:
    location: classpath:/maigaoCloud.jks #证书（密钥）库文件
    password: maigao                     #证书（密钥）库口令
    alias: maigaoCloud                   #证书（密钥）别名
    secret: maigao                       #证书（密钥）口令